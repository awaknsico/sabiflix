# v0 sandbox internal files
__v0_runtime_loader.js
__v0_devtools.tsx
__v0_jsx-dev-runtime.ts
.snowflake/
.v0-trash/
.vercel/

# Environment variables
.env*.local

# Runtime published-catalog store (JSON stand-in for the DB)
.data/

# Diagnostic / build logs and TS incremental build cache
*.log
*.tsbuildinfo

# OpenNext (Cloudflare) build output
.open-next/

# Throwaway Clerk-vs-Next integration test app (not part of the product)
clerk-nextjs/

# This repo uses npm (package-lock.json is tracked). The old pnpm lockfiles
# were removed - they had drifted out of sync with package.json and broke
# Cloudflare Workers Builds' frozen installs.

# Common ignores
node_modules
.next/
.DS_Store